<div class="container">
    <div class="row">
        <div class="col-md-12">
          <h2><%= @post.title %></h2>
            <h4>&emsp;<i>by <%= @author.name %></i></h4>
            <p><%= @post.body %></p>
            <%= link_to "Back", user_path(@user) %>
        </div>
    </div>
</div>
